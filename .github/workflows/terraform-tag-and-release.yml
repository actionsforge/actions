name: terraform-tag-and-release

on:
  workflow_call:

permissions:
  contents: write

jobs:
  release:
    name: Terraform Tag and Release
    uses: ./.github/workflows/terraform-tag.yml
    with:
      create-release: true
    secrets: inherit
